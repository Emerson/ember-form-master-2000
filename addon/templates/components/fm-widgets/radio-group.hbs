{{#each widgetAttrs.content as |option|}}
  <FmField @widget="radio" @display="radio" @value={{value}} @targetValue={{get option widgetAttrs.optionValuePath}} @label={{get option widgetAttrs.optionLabelPath}} @onUserInteraction={{action "radioButtonInteraction"}} />
{{/each}}
